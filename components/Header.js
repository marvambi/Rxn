import React from 'react';
import {Text, SafeAreaView} from 'react-native';

const Header = () => {
  return (
    <SafeAreaView>
      <Text>Header</Text>
    </SafeAreaView>
  );
};

export default Header;
